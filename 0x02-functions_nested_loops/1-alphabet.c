#include "main.h"
/**
 * main  - a function that prints the alphabet, in lowercase
 *
 * Return: Always successfull
 */
int main(void)
{
	int i;

	for (i = 'a'; i < 'z'; i++)
	{
	_putchar(i);
	}
	_putchar('\n');
	return (0);
}
