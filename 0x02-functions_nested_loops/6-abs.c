#include "main.h"
/**
 * _abs - Function that computes the absolute value
 * of an integer.
 *
 * @n: parameter
 * Return: Absolute value
 */
int _abs(int n)
{
	if (n > 0)
	{
		return (n);
	}
	else
	{
		return (n * -1);
	}
}
