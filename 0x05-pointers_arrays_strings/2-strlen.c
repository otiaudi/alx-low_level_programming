#include "main.h"
#include <string.h>
/**
 * _strlen -> Function that returns the length of string
 * @s: The parameter to ne returned
 * Return: Returns the length of the string
 */
int _strlen(char *s)
{
	return (strlen(s));
}



